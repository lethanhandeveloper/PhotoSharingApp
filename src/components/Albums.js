import React from 'react';
import { Text, View } from 'react-native';

const Albums = () => {
    return (
        <View>
            <Text>Albums</Text>
        </View>
    );
};

export default Albums;